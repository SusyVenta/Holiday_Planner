run --> edit configurations
runserver


# after creatign a model, to create post, get etc.: makemigrations
# to see sql that is run when executing a request: sqlmigrate finance 0001
# to apply migrations: migrate
# shell: from finance.models import Post
            from django.contrib.auth.models import User
            to see all users: User.objects.all()
            see first user: User.objects.first()
            User.objects.get(id=1)
            User.objects.filter(username="Susy").last()
            User.pk
            User.id


#to create project: py -3.6 -m django startproject aws_file_upload
# to add module/app: py -3.6 manage.py startapp upload_section


# create database/ detect changes/ how to update database: manage.py makemigrations --> plans\migrations\0001_initial.py
# every time i create new models, need to run makemigrations
# to actually update database: manage.py migrate

#to create admin account: py -3.6 manage.py createsuperuser


http://127.0.0.1:8000/upload_section/



py -3.6 manage.py dbshell